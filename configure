#!/usr/bin/env bash

NAME=$(basename $0)
SCRIPT=$(realpath $0)
SCRIPT_DIR=$(dirname $SCRIPT)
. $SCRIPT_DIR/environment

# copy ini file
$CP_COMMAND $SCRIPT_DIR_ETC/php/php.ini $PHP_INI_DIR/

# copy fpm configuration file
$CP_COMMAND $SCRIPT_DIR_ETC/php/php-fpm.conf $PHP_INI_DIR/

